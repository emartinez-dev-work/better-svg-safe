{
  "name": "better-svg-safe",
  "displayName": "Better SVG Safe",
  "description": "Preview, inspect, and safely optimize SVGs with color, zoom, Code Interactive, and SVGO controls.",
  "version": "0.4.4",
  "publisher": "emartinez-dev-work",
  "author": {
    "name": "emartinez-dev-work",
    "url": "https://github.com/emartinez-dev-work"
  },
  "pricing": "Free",
  "license": "Apache-2.0",
  "icon": "./static/icon.png",
  "galleryBanner": {
    "color": "#df6c31",
    "theme": "dark"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/emartinez-dev-work/better-svg-safe"
  },
  "homepage": "https://github.com/emartinez-dev-work/better-svg-safe#readme",
  "qna": false,
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "vscode": "^1.85.0",
    "node": ">=18.15.0"
  },
  "activationEvents": [
    "onLanguage:astro",
    "onLanguage:django-html",
    "onLanguage:ejs",
    "onLanguage:erb",
    "onLanguage:html",
    "onLanguage:javascript",
    "onLanguage:javascriptreact",
    "onLanguage:php",
    "onLanguage:svelte",
    "onLanguage:svg",
    "onLanguage:typescript",
    "onLanguage:typescriptreact",
    "onLanguage:vue",
    "onLanguage:xml",
    "onLanguage:liquid"
  ],
  "categories": [
    "Programming Languages",
    "Visualization",
    "Other"
  ],
  "keywords": [
    "svg",
    "svg-preview",
    "svg-editor",
    "svg-optimization",
    "svgo",
    "vector-graphics",
    "graphics",
    "visualization",
    "preview",
    "optimize"
  ],
  "main": "./dist/extension.js",
  "files": [
    "static/icon.png",
    "static/screenshot.png",
    "dist/webview",
    "dist/extension.js",
    "package.json",
    "README.md",
    "LICENSE",
    "NOTICE",
    "CHANGELOG.md"
  ],
  "contributes": {
    "views": {
      "explorer": [
        {
          "type": "webview",
          "id": "betterSvg.preview",
          "name": "SVG Preview",
          "icon": "$(eye)",
          "when": "betterSvg.hasSvgOpen"
        }
      ]
    },
    "languages": [
      {
        "id": "liquid",
        "aliases": [
          "Liquid"
        ],
        "extensions": [
          ".liquid"
        ]
      }
    ],
    "commands": [
      {
        "command": "betterSvg.optimize",
        "title": "Optimize SVG",
        "category": "Better SVG Safe",
        "icon": "$(zap)"
      },
      {
        "command": "betterSvg.optimizeFromHover",
        "title": "Optimize SVG from Hover",
        "category": "Better SVG Safe",
        "icon": "$(zap)"
      }
    ],
    "menus": {
      "commandPalette": [
        {
          "command": "betterSvg.optimize",
          "when": "editorTextFocus && resourceExtname == .svg"
        }
      ],
      "editor/title": [
        {
          "command": "betterSvg.optimize",
          "when": "resourceExtname == .svg",
          "group": "navigation"
        }
      ]
    },
    "configuration": {
      "title": "Better SVG Safe",
      "properties": {
        "betterSvg.autoReveal": {
          "type": "boolean",
          "default": true,
          "description": "Automatically reveal and expand the SVG Preview panel in Explorer when opening SVG files"
        },
        "betterSvg.autoCollapse": {
          "type": "boolean",
          "default": true,
          "description": "Automatically collapse the SVG Preview panel in Explorer when closing all SVG files"
        },
        "betterSvg.defaultColor": {
          "type": "string",
          "default": "#ffffff",
          "pattern": "^#([A-Fa-f0-9]{6}|[A-Fa-f0-9]{3})$",
          "description": "Default color for currentColor in the SVG preview (hex format, e.g., #ffffff)"
        },
        "betterSvg.removeClasses": {
          "type": "boolean",
          "default": true,
          "description": "Remove class attributes from SVG elements during optimization"
        },
        "betterSvg.enableHover": {
          "type": "boolean",
          "default": true,
          "description": "Enable hover preview for SVG elements in supported files"
        },
        "betterSvg.showGutterPreview": {
          "type": "boolean",
          "default": true,
          "description": "Show SVG preview icons next to line numbers in supported files"
        }
      }
    },
    "configurationDefaults": {
      "workbench.editorAssociations": {
        "*.svg": "default"
      }
    }
  },
  "scripts": {
    "pnpm": "pnpm",
    "vscode:prepublish": "pnpm package",
    "compile": "pnpm check-types && node esbuild.js && pnpm copy-webview",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "watch:webview": "node -e \"require('fs').watch('src/webview', () => require('child_process').exec('pnpm copy-webview'))\"",
    "package": "pnpm check-types && node esbuild.js --production && pnpm copy-webview",
    "check": "pnpm check-types",
    "check-types": "tsc --noEmit",
    "copy-webview": "node -e \"const fs=require('fs'),path=require('path');fs.mkdirSync('dist/webview',{recursive:true});['html','css','js'].forEach(ext=>fs.readdirSync('src/webview').filter(f=>f.endsWith('.'+ext)).forEach(f=>fs.copyFileSync(path.join('src/webview',f),path.join('dist/webview',f))))\"",
    "test": "node --import tsx --test src/*.test.ts",
    "test:vscode": "vscode-test",
    "lint": "eslint src"
  },
  "devDependencies": {
    "@types/node": "^18.15.0",
    "@types/vscode": "^1.85.0",
    "@typescript-eslint/eslint-plugin": "8.59.1",
    "@typescript-eslint/parser": "8.59.1",
    "esbuild": "0.28.0",
    "eslint": "9.39.4",
    "neostandard": "0.13.0",
    "npm-run-all": "4.1.5",
    "tsx": "4.21.0",
    "typescript": "5.9.3"
  },
  "dependencies": {
    "svgo": "4.0.1"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
